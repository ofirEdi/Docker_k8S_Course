sudo: required
services:
 - docker

before_before_install:
  - docker build -t ofiredi/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true ofiredi/docker-react npm run test -- --coverage